IF EXISTS (SELECT * FROM CAPUSERS.Users
           WHERE username='ADMIN') THEN
  DELETE FROM CAPUSERS.Users WHERE username='ADMIN';
END IF;

INSERT INTO CAPUSERS.Users
    (username,
     name,
     passwd,
     userdescription,
     lastlogin,
     lastpasswordchange)
VALUES
    ('ADMIN',
     'CAP Administrator',
     PASSWORD('admin1234'),
     'Super user',
     CURTIME(),
     CURTIME());
